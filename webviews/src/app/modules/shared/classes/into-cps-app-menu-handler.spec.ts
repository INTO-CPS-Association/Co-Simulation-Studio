import { IntoCpsAppMenuHandler } from './into-cps-app-menu-handler';

describe('IntoCpsAppMenuHandler', () => {
  it('should create an instance', () => {
    expect(new IntoCpsAppMenuHandler()).toBeTruthy();
  });
});
