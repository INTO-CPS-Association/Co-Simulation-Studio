import { CoSimulationConfig } from './co-simulation-config';

describe('CoSimulationConfig', () => {
  it('should create an instance', () => {
    expect(new CoSimulationConfig()).toBeTruthy();
  });
});
